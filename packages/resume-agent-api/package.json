{
  "name": "@yamlresume/resume-agent-api",
  "version": "0.12.2",
  "private": true,
  "description": "HTTP API for YAMLResume's job-tailoring agent",
  "license": "MIT",
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "build:clean": "rm -rf dist",
    "dev": "tsx src/server.ts",
    "test": "vitest --run",
    "test:cov": "vitest --coverage --run",
    "test:watch": "vitest",
    "lint": "biome check src"
  },
  "dependencies": {
    "@yamlresume/resume-agent": "workspace:*",
    "busboy": "^1.6.0",
    "nodemailer": "^10.0.10",
    "tslib": "^2.8.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4",
    "@types/node": "^24.4.0",
    "@types/nodemailer": "^8.0.2",
    "yaml": "^2.8.2"
  }
}
